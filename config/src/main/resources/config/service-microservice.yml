spring:
  application:
    name: service-microservice
  datasource:
    url: ${DB_MS_SERVICE_URL}

server:
  port: ${SERVICE_SERVER_PORT}

eureka:
  instance:
    preferIpAddress: false
    hostname: service-microservice
    instance-id: service-microservice

info:
    app:
        name: Service Microservice
        description: Service Microservice
        version: 1.0.0
    company:
        name: Turismo
    build:
        time: ${BUILD_TIME:2024-08-21T12:30:00Z}